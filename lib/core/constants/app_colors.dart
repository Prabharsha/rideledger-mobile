import 'package:flutter/material.dart';

/// App color palette
class AppColors {
  // Primary colors
  static const primary = Color(0xFF1976D2); // Material blue
  static const primaryDark = Color(0xFF1565C0);
  static const primaryLight = Color(0xFF42A5F5);

  // Accent colors
  static const accent = Color(0xFFFF6F00); // Orange for TW200 theme
  static const accentLight = Color(0xFFFFB74D);

  // Semantic colors
  static const success = Color(0xFF4CAF50); // Green
  static const warning = Color(0xFFFBC02D); // Yellow
  static const error = Color(0xFFF44336); // Red
  static const info = Color(0xFF2196F3); // Blue

  // Background colors
  static const darkBg = Color(0xFF121212);
  static const darkSurface = Color(0xFF1E1E1E);
  static const darkSurfaceVariant = Color(0xFF2C2C2C);

  static const lightBg = Color(0xFFFAFAFA);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightSurfaceVariant = Color(0xFFF5F5F5);

  // Text colors
  static const darkText = Color(0xFF212121);
  static const lightText = Color(0xFFFAFAFA);
  static const mutedText = Color(0xFF757575);

  // Gauge colors
  static const gaugeGreen = Color(0xFF4CAF50);
  static const gaugeYellow = Color(0xFFFBC02D);
  static const gaugeRed = Color(0xFFF44336);
  static const gaugeLow = Color(0xFFB0BEC5);

  // Speed display colors
  static const speedNormal = Color(0xFF4CAF50);
  static const speedWarning = Color(0xFFFBC02D);
  static const speedDanger = Color(0xFFF44336);

  // Dashboard colors
  static const dashboardBg = Color(0xFF000000);
  static const dashboardText = Color(0xFFFFFFFF);
  static const dashboardAccent = Color(0xFFFF6F00);

  // Gradient colors
  static const gradientStart = Color(0xFF1976D2);
  static const gradientEnd = Color(0xFF0D47A1);
}
